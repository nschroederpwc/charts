    questions:
    - variable: service.port
    default: "80"
    description: "Port for the service"
    type: string
    required: true
    label: WHAT SERVICE PORT?
    show_subquestion_if: true
    group: "Service settings"
